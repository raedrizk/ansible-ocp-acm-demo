---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: foxhound-webserver
  name: foxhound-webserver
spec:
  ports:
  - name: 8080-tcp
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: foxhound-webserver
  sessionAffinity: None
  type: ClusterIP
